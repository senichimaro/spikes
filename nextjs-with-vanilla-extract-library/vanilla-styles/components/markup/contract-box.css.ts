import { createThemeContract } from "@vanilla-extract/css";

export const contractBox = createThemeContract({
    boxes: {
      display: null,
      justifyContent: null,
      flexFlow: null,
      alignItems: null,
    },
  });